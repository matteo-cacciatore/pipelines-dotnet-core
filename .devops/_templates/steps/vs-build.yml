parameters:
- name: baseFolder
  type: string
- name: codeFolder
  type: string

steps:
  # Questa, va a compilare il progetto in modalità release
  # - script: dotnet build --configuration Release
  #   displayName: 'Build project'
  - task: Bash@3
    displayName: Extract Artifact Filename
    name: GetArtifact
    inputs:
      targetType: inline
      script: |
        ls -al

  - task: VSBuild@1
    inputs:
      solution: '$(System.DefaultWorkingDirectory)/${{ parameters.baseFolder }}/${{ parameters.codeFolder }}/*.csproj'
      configuration: 'Release'
      clean: true
