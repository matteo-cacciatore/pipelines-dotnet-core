# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml
 
variables:
  baseFolder: Integration/WebService_FrontEnd
  codeFolder: WEBSERVICE/Adesioni/Reply.GRM.AdesioniServiceData.WebUI
  webAppName: Adesioni

trigger:
- devops

pool:
  vmImage: windows-latest

stages:
- stage: Build
  jobs:
  - job: Buildjob
    - template: ../../_templates/steps/versioning.yml